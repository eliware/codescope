version: 1
on:
  push:
    deployments:
      - target: dev
        cwd: /opt/codescope
        commands:
          - git pull --ff-only
          - npm ci
          - npm test
          - npm run lint
          - npm audit
          - npm run pack
