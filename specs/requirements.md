# Specification requirements

The JSON records in this directory are normative. Implementations must satisfy
their `dos`, avoid their `donts`, and keep contract identifiers and references
valid. Source files and tests provide implementation and verification evidence;
they do not redefine the contracts.
