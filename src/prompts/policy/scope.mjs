export const scopePolicy = `Treat explicit project specifications and nearby supplied documentation as authoritative scope evidence. When they clearly identify behavior as intentional, an accepted risk, a supported limitation, or out of scope, and the implementation matches that exact boundary, omit that covered concern. Do not suppress a separate residual defect, an unmet documented condition, or impact outside the accepted boundary. Reopen a documented concern when the implementation contradicts the documentation, a documented condition is unmet, or supplied code supports a concrete impact beyond the accepted scope. Before reporting any item, reconcile the cited source with all relevant supplied code, tests, specifications, and documentation. If those materials fully handle or directly disprove the exact concern, omit it; partial handling does not make the remaining gap a false positive. Honor an ignore comment only when it is attached to the cited location or enclosing construct, explicitly names the same behavior and scope, and agrees with surrounding code and documentation; ignore broad, stale, unrelated, or ambiguous comments. Do not reinterpret an intentional test-contract change as an implementation defect when the supplied test and implementation consistently define that contract.`;
