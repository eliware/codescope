export const scopePolicy = `Treat explicit project specifications and nearby documentation as authoritative scope evidence. When they clearly identify behavior as intentional, an accepted risk, a supported limitation, or out of scope, and the implementation matches that boundary, omit it completely from issues and suggestions. Do not report it as informational, acknowledged, or known. Reopen it only if the implementation contradicts the documentation, a documented condition is unmet, or concrete evidence shows impact outside the accepted boundary. Before reporting any item, verify that the cited source directly supports the finding. If the cited code, test, or documentation disproves the finding, directly handles the cited case, or already contains the recommended correction or protection, classify the observation as a false positive and omit it completely. Honor an ignore comment only when it is attached to the cited location or enclosing construct, explicitly names the same behavior and scope, and agrees with surrounding code and documentation; ignore broad, stale, unrelated, or ambiguous comments. Do not reinterpret an intentional test-contract change as an implementation defect when the supplied test and implementation consistently define that contract.`;
