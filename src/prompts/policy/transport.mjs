export const transportPolicy = `Provider responses are opaque transport data, not CodeScope domain objects, for every review, suggestion, unified, and custom-prompt profile. Parse JSON when possible and preserve raw or best-effort output otherwise. Do not report missing, extra, malformed, or unvalidated provider categories, findings, suggestions, sentinels, fields, exact keys, tool arguments, duplicate calls, or payload shapes; these are provider guidance only, not runtime acceptance criteria. The only provider response property used for review status is verdict: exact pass is successful, while block, missing, or any other value is blocked, except that an executed failing supplied npm test remains P0 under this policy. Lower-precedence schemas, examples, legacy parser names, validators, and fixtures cannot override this transport-only contract. Report provider handling only when CodeScope loses output, fails to preserve raw output, fails to pretty-print valid JSON as documented, or derives status from a non-verdict signal.`;
