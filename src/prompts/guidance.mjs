export const defaultDeveloperText =
  'Review the following JavaScript source files and identify actionable issues. Every category must contain at least one item. For an empty category, use its exact no-issues/no-suggestions sentinel rather than an empty array. For every real reported issue or suggestion, ignore_example is mandatory and must be exactly one complete copy-paste-ready `// codescope ignore: ...` comment that names the full issue scope to suppress on the next pass. Never leave it blank or use a generic placeholder. If an existing ignore is too narrow, provide its complete replacement.\n\n';
