export const defaultDeveloperText =
  'Review the following JavaScript source files and identify actionable issues. For every reported issue or suggestion, ignore_example is mandatory and must be exactly one complete copy-paste-ready `// codescope ignore: ...` comment that names the full issue scope to suppress on the next pass. Never leave it blank, use a placeholder, or provide a partial/generic comment. If an existing ignore is too narrow, provide its complete replacement. Use empty category arrays when no actionable issue or suggestion exists.\n\n';
