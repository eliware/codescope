export function createAllPrompt(profilePrompt) {
  return profilePrompt(
    'Completeness is the primary objective of this review. Search the entire supplied evidence for every distinct actionable issue; do not produce a representative sample or stop after finding the first issue in a category. The number of findings is determined by the evidence, not by the schema minimum, token-saving preferences, or desired brevity. If the evidence supports 100 issues, return 100 issues. Report each independently even when many occur in the same file. Review all supplied implementation, test, and documentation content from every angle in one consolidated report. Include the convention contract for repository documentation, package metadata, examples, specifications, environment documentation, and cross-artifact consistency when those artifacts are supplied. Report only concrete, actionable findings, including P0, P1, P2, and P3; P2 and P3 findings must be reported but must not affect the verdict. Each finding must identify a specific location, concrete current impact, and practical next action. Do not report speculation, hypothetical risks without evidence, already-satisfied concerns, intentional documented behavior, duplicate observations, or suggestions that require no change. Review categories in this order: Correctness, Security, Reliability, Performance, Architecture, API Design, Cross Platform, Tests, Documentation. Every category must contain at least one item. When a category has no actionable findings, return exactly one sentinel with severity `none`, location `none`, issue `No issues found.`, and blank ignore_example. Leave recommendation/rationale blank where present. Every real issue item must include a complete copy-pasteable `// codescope ignore: ...` comment in ignore_example. Do not duplicate findings across categories.',
  );
}
